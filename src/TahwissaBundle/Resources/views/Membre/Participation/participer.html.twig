{% extends "TahwissaBundle:Membre:base.html.twig" %}

{% block title %}
    Participer <br><br>
{% endblock %}
{% block content %}
<script>
</script>
<div id="zone1"><h1> {{ message }} </h1></div>
    <form action="{{ path('part_event', {"id":evenement.id }) }}" method="post">
        <div class="form-group">
            <input type="password" class="form-control" id="pwd" name="mdp" placeholder="Mot de pass" required>
        </div>
        <div class="form-group">
            <input maxlength="3" type="number" value="{{ nbplaces }}" min="1" max="3" step="1" data-number-to-fixed="1" data-number-stepfactor="3" class="form-control currency" name="nbplaces"  required/>
        </div>
        <div class="form-group">
            <label class="sr-only" for="exampleInputAmount">Amount (in dollars)</label>
            <div class="input-group">
                <div class="input-group-addon">$</div>
                <input type="text" class="form-control" id="exampleInputAmount" placeholder="le solde va etre calculer automatiquement" value="{{ prix * nbplaces }}" disabled>
            </div>
            <h6>le solde va etre calculer automatiquement</h6>
        </div>
        <button type="submit" class="btn btn-success" onclick="affichemessage()">Transfer cash</button>
    </form>
{% endblock %}
